# configs

Configuration files for analysis runs.

Expected contents may include paths to project areas, hazard layers, population rasters, vulnerability layers, output directories, and indicator-specific parameters. Use `example.yml` as the template once the first wetland workflow is implemented.
