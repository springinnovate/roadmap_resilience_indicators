# data

Local data workspace for raw, interim, and processed datasets.

Large datasets should not be committed to git. Document source URLs, access notes, licenses, download dates, and expected file names here as datasets are added.

Suggested local subdirectories:

- `raw/`: Original downloaded or received datasets.
- `interim/`: Cleaned or harmonized intermediate files.
- `processed/`: Analysis-ready layers.
